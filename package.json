{
  "name": "pi-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'nothing to build'",
    "test": "vitest run",
    "lint": "oxlint",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "check": "vitest run && oxlint && oxfmt --check ."
  },
  "dependencies": {
    "@mariozechner/pi-ai": "^0.61.1",
    "@mariozechner/pi-coding-agent": "^0.61.1",
    "@sinclair/typebox": "^0.34.41",
    "jsdom": "^26.0.0",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "oxfmt": "^0.41.0",
    "oxlint": "^1.56.0",
    "vitest": "^4.1.0"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
